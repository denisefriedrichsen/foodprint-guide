<div class="container-fluid">
  <%= render 'shared/banner-home' %>
  <div class="home-button">
    <%= simple_form_for :search, url: root_path, method: :get do |f| %>
      <div class="search-form-control form-group">
        <input class="form-control string required" placeholder="Your city" type="text" name="search[query]" id="search_query" />
        <button name="button" type="submit" class="btn btn-gold">
         GO!
        </button>
      </div>
    <% end %>
  </div>
</div>
